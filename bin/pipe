#! /usr/bin/env node

var pipe = require('fcpipe')
var argv = process.argv;

if (argv.length < 3) {
    pipe.start(8000);
} else {
    if (+argv[2]) {
        pipe.start(+argv[2]);
    } else {
        staticHost = argv[2];
        pipe.start(8000, argv[2]);
    }
}